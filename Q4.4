function arrSum(arr) {
 let sum = 0;
 for (let i = 0; i < arr.length; ++i) {
    // This is how you loop throug an array
    sum = sum + arr[i];    
}
return sum
}



console.log(arrSum([1, 2, 3, 4, 5, 6, ]))
